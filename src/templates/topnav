
 <section id="hidden-menu">

        <div class="center">
           <div class="circle">
            <div class="user_image">
              <a target="_blank" href="{{user.html_url}}">
                <img src="{{user.avatar_url}}">
              </a>
            </div>
            </div>
            <ul>
                <li><a href="#team">Team</a></li>
                <li><a href="#toolbelt">Tool Belt</a></li>
            </ul>
        </div>

</section>

 <nav class="navbar navbar-fixed-top navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#" id="call-menu"></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Projects <span class="caret"></span></a>
        </li> -->
        {{#isAdmin}}
        <li><a href="#settings">Settings</a></li>
        {{/isAdmin}}
        <li><a href="#team">Team</a></li>
        <li><a href="#toolbelt">Tool Belt</a></li>
        <li class="user_holder">
            <div class="user_description">
                <p class="user_name">{{user.login}}</p>
                <p class="user_org">{{organization}}</p>
            </div>
            <div class="user_image">
                <a target="_blank" href="{{user.html_url}}">
                  <img src="{{user.avatar_url}}">
                </a>
            </div>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="center">
    <h1>#PROJECTNAME</h1>
    <p class="sub_title"> Dive into new languages, frameworks and hardware!</p>
</div>
